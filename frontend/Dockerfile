# Use an official Node.js runtime as a base image
FROM node:16-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# This is just a placeholder, so no further commands are necessary.
# More meaningful commands will be added as you start developing your frontend app.
